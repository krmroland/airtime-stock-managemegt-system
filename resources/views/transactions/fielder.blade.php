<div class="d-flex justify-content-around align-items-center">
<img src="{{$fielder->imagePath }}" class="img-thumbnail rounded-circle image-70 mb-1">
	<p class="h5 text-muted">{{ $fielder->name }}</p>
	<p class="h5 text-muted">{{ $fielder->phoneNumber }}</p>
</div>